<h2>タスク一覧</h2>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= task.title %>
    <%= link_to "詳細", task %>
    <%= link_to "編集", edit_task_path(task) %>
    <%= link_to "削除", task, method: :delete %>
  </li>
  <% end %>
</ul>

<%= link_to "新規登録", new_task_path %>
